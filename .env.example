# =============================================================================
# All env lives at REPO ROOT (.env). Copy this file to .env at the repo root.
# Server, drizzle-kit, and scripts load it from there so one file covers the monorepo.
# =============================================================================

# Local dev: create user and DB if needed, e.g. createuser mycoach -P && createdb -O mycoach mycoach
DATABASE_URL=postgresql://mycoach:mycoach@localhost:5432/mycoach
# Frontend: Hono server URL (for auth + tRPC)
NEXT_PUBLIC_SERVER_URL=http://localhost:3001
BETTER_AUTH_SECRET=change-me-to-a-random-secret
BETTER_AUTH_URL=http://localhost:3001
WEB_URL=http://localhost:3000
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=
# Ollama (dev): base URL; chat uses /v1 (auto-normalized), embeddings use /api/embed
OLLAMA_BASE_URL=http://localhost:11434/v1
# Ollama model for chat (e.g. llama3.1:8b)
OLLAMA_MODEL=llama3.1:8b
# Embeddings: with OPENAI_API_KEY use OpenAI; without, use Ollama (run: ollama pull nomic-embed-text)
OLLAMA_EMBED_MODEL=nomic-embed-text
# Optional: override coaching models (defaults to ollama:OLLAMA_MODEL)
# COACH_CHAT_MODEL=ollama:llama3.1:8b
# COACH_FACT_MODEL=ollama:llama3.1:8b

# 32-byte hex key for encrypting user API keys. Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY_ENCRYPTION_KEY=

# E2E (Playwright): test users for e2e/01-foundation.spec.ts
TEST_USER_EMAIL=
TEST_USER_PASSWORD=
TEST_USER_EMAIL_2=
TEST_USER_PASSWORD_2=
