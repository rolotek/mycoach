---
phase: 05-ui-polish-styling
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/web/src/app/(auth)/login/page.tsx
  - apps/web/src/app/(auth)/signup/page.tsx
  - apps/web/src/app/(app)/dashboard/page.tsx
  - apps/web/src/app/(app)/settings/page.tsx
autonomous: true
must_haves:
  truths:
    - "Login page has a centered card with shadcn/ui Input, Label, and Button components"
    - "Signup page matches login page styling with consistent card layout"
    - "Dashboard shows a welcome card grid with icon-labeled navigation cards"
    - "Settings page uses shadcn/ui Select components for provider/model dropdowns"
    - "All four pages use semantic design tokens (bg-background, text-foreground, bg-primary) instead of raw Tailwind colors"
    - "Error states use destructive color tokens"
    - "Loading states use Skeleton components instead of plain text"
  artifacts:
    - path: "apps/web/src/app/(auth)/login/page.tsx"
      provides: "Polished login form with shadcn/ui components"
    - path: "apps/web/src/app/(auth)/signup/page.tsx"
      provides: "Polished signup form matching login design"
    - path: "apps/web/src/app/(app)/dashboard/page.tsx"
      provides: "Dashboard with card-based navigation grid"
    - path: "apps/web/src/app/(app)/settings/page.tsx"
      provides: "Settings with shadcn/ui Select and Card"
  key_links:
    - from: "apps/web/src/app/(auth)/login/page.tsx"
      to: "@/components/ui/button"
      via: "import Button"
      pattern: "import.*Button.*from.*@/components/ui/button"
    - from: "apps/web/src/app/(app)/dashboard/page.tsx"
      to: "@/components/ui/card"
      via: "import Card components"
      pattern: "import.*Card.*from.*@/components/ui/card"
    - from: "apps/web/src/app/(app)/settings/page.tsx"
      to: "@/components/ui/select"
      via: "import Select components"
      pattern: "import.*Select.*from.*@/components/ui/select"
---

<objective>
Restyle the auth pages (login, signup), dashboard, and settings using shadcn/ui components and design tokens.

Purpose: These four pages are the simplest to restyle (forms and cards with no complex interactions) and establish the pattern for how all pages use the design system.

Output: Four polished, consistent pages using shadcn/ui Button, Card, Input, Label, Select, and Skeleton components with semantic color tokens.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ui-polish-styling/05-RESEARCH.md
@.planning/phases/05-ui-polish-styling/05-01-SUMMARY.md
@apps/web/src/app/(auth)/login/page.tsx
@apps/web/src/app/(auth)/signup/page.tsx
@apps/web/src/app/(app)/dashboard/page.tsx
@apps/web/src/app/(app)/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle login and signup pages with shadcn/ui components</name>
  <files>
    apps/web/src/app/(auth)/login/page.tsx
    apps/web/src/app/(auth)/signup/page.tsx
  </files>
  <action>
    Restyle both auth pages to use shadcn/ui components. They should be visually identical in structure (centered card, consistent spacing).

    **Login page (`apps/web/src/app/(auth)/login/page.tsx`):**
    - Replace the outer div with `min-h-screen flex items-center justify-center bg-background p-4`
    - Replace the card div with shadcn/ui `<Card className="w-full max-w-md">` + CardHeader + CardContent + CardFooter
    - CardHeader: `<CardTitle>Sign in</CardTitle>` + `<CardDescription>Enter your credentials to continue</CardDescription>`
    - CardContent: the form
    - Replace all `<label>` with shadcn/ui `<Label htmlFor="...">`
    - Replace all `<input>` with shadcn/ui `<Input>` (keep all existing props: id, type, value, onChange, required, autoComplete)
    - Replace the submit `<button>` with `<Button type="submit" className="w-full" disabled={loading}>`
    - Replace error display: use `bg-destructive/10 text-destructive` instead of `bg-red-50 text-red-600`
    - Replace social login buttons with `<Button variant="outline" className="w-full">` with appropriate icon or text
    - Add a `<Separator>` between form and social buttons with "or" text (use `<div className="relative my-6"><Separator /><span className="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 bg-card px-2 text-xs text-muted-foreground">or</span></div>`)
    - CardFooter: the "Don't have an account? Sign up" link
    - Remove ALL raw Tailwind color classes (bg-blue-600, text-neutral-*, border-neutral-*, etc.) -- use only semantic tokens
    - Keep all existing logic (handleSubmit, handleSocial, state) exactly the same

    **Signup page (`apps/web/src/app/(auth)/signup/page.tsx`):**
    - Apply identical Card/CardHeader/CardContent/CardFooter structure
    - Same component replacements (Label, Input, Button)
    - CardTitle: "Create account", CardDescription: "Get started with MyCoach"
    - Has 3 fields (name, email, password) instead of 2
    - No social login section (keep as-is -- signup only has email/password)
    - CardFooter: "Already have an account? Sign in" link
    - Keep all existing logic unchanged

    **Imports needed for both:** Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter from @/components/ui/card; Button from @/components/ui/button; Input from @/components/ui/input; Label from @/components/ui/label; Separator from @/components/ui/separator (login only).
  </action>
  <verify>
    - `cd apps/web && npx tsc --noEmit` passes
    - `grep "bg-blue-600\|text-neutral-\|border-neutral-" apps/web/src/app/\\(auth\\)/login/page.tsx` returns NO matches (all raw colors replaced)
    - `grep "bg-blue-600\|text-neutral-\|border-neutral-" apps/web/src/app/\\(auth\\)/signup/page.tsx` returns NO matches
    - `grep "import.*Button" apps/web/src/app/\\(auth\\)/login/page.tsx` returns match
    - `grep "import.*Card" apps/web/src/app/\\(auth\\)/login/page.tsx` returns match
  </verify>
  <done>
    Login and signup pages use shadcn/ui Card, Input, Label, Button, and Separator components with semantic design tokens. No raw Tailwind color classes remain. All existing auth logic (email/password, OAuth, routing) works unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle dashboard and settings pages with shadcn/ui components</name>
  <files>
    apps/web/src/app/(app)/dashboard/page.tsx
    apps/web/src/app/(app)/settings/page.tsx
  </files>
  <action>
    **Dashboard page (`apps/web/src/app/(app)/dashboard/page.tsx`):**
    - Remove the `min-h-screen bg-neutral-50 p-8` wrapper and `mx-auto max-w-2xl` -- the app shell layout now handles this. Replace with `<div className="p-4 md:p-6 space-y-6">`.
    - Use PageHeader component: `<PageHeader title={`Welcome, ${session.user?.name ?? "User"}`} description={session.user?.email ?? ""} />`
    - Replace the link buttons with a card grid layout using shadcn/ui Cards:
      ```
      <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      ```
    - Each navigation item becomes a clickable Card (wrapped in Link):
      - Start Coaching Session (/chat) -- MessageSquare icon, primary card styling
      - Memory & Knowledge (/memory) -- Brain icon
      - Documents (/documents) -- FileText icon
      - Agents (/agents) -- Bot icon
      - Settings (/settings) -- Settings icon
    - Each Card: `<Card className="group cursor-pointer transition-colors hover:bg-accent">` with CardHeader containing an icon + title, and a brief CardDescription
    - Use lucide-react icons: MessageSquare, Brain, FileText, Bot, Settings
    - Sign out button: `<Button variant="ghost" onClick={handleSignOut}>Sign out</Button>` placed in the PageHeader actions slot
    - Remove ALL raw Tailwind color classes

    **Settings page (`apps/web/src/app/(app)/settings/page.tsx`):**
    - Remove the `min-h-screen bg-neutral-50 p-8` wrapper and `mx-auto max-w-2xl`. Replace with `<div className="p-4 md:p-6 space-y-6 max-w-2xl">`.
    - Use PageHeader: `<PageHeader title="Settings" description="Choose your preferred LLM provider and model." />`
    - Remove the "Back to dashboard" link -- the sidebar handles navigation now
    - Wrap the form in shadcn/ui `<Card>` + CardHeader("LLM Configuration") + CardContent + CardFooter
    - Replace `<select>` elements with shadcn/ui `<Select>` + SelectTrigger + SelectValue + SelectContent + SelectItem
    - Replace `<label>` with `<Label>`
    - Replace save `<button>` with `<Button>` in CardFooter
    - Success message: use `text-sm text-muted-foreground` or a green Badge
    - Error message: use `text-sm text-destructive`
    - Loading state: return `<div className="p-4 md:p-6"><Skeleton className="h-8 w-48" /><Skeleton className="mt-4 h-64 w-full max-w-2xl" /></div>` instead of plain "Loading settings..."
    - Remove ALL raw Tailwind color classes

    **Imports needed:**
    - Dashboard: Card, CardHeader, CardTitle, CardDescription from @/components/ui/card; Button from @/components/ui/button; PageHeader from @/components/page-header; lucide-react icons
    - Settings: Card, CardHeader, CardTitle, CardContent, CardFooter from @/components/ui/card; Button from @/components/ui/button; Label from @/components/ui/label; Select, SelectTrigger, SelectValue, SelectContent, SelectItem from @/components/ui/select; Skeleton from @/components/ui/skeleton; PageHeader from @/components/page-header
  </action>
  <verify>
    - `cd apps/web && npx tsc --noEmit` passes
    - `grep "bg-blue-600\|bg-neutral-50\|text-neutral-\|border-neutral-" apps/web/src/app/\\(app\\)/dashboard/page.tsx` returns NO matches
    - `grep "bg-blue-600\|bg-neutral-50\|text-neutral-\|border-neutral-" apps/web/src/app/\\(app\\)/settings/page.tsx` returns NO matches
    - `grep "import.*Select" apps/web/src/app/\\(app\\)/settings/page.tsx` returns match
    - `grep "PageHeader" apps/web/src/app/\\(app\\)/dashboard/page.tsx` returns match
  </verify>
  <done>
    Dashboard displays a card grid with icons for navigation, using PageHeader and shadcn/ui Cards. Settings uses shadcn/ui Select components for provider/model dropdowns with Card layout. Both pages use semantic design tokens with no raw color classes. All existing functionality (sign out, provider selection, save) works unchanged.
  </done>
</task>

</tasks>

<verification>
- `cd apps/web && npm run build` completes without errors
- All four pages (login, signup, dashboard, settings) use shadcn/ui components
- No raw Tailwind color classes (bg-blue-*, text-neutral-*, border-neutral-*) remain in any of the four files
- Auth pages render centered cards with proper Input/Label/Button components
- Dashboard shows a card grid with lucide-react icons
- Settings uses Select components for dropdowns
</verification>

<success_criteria>
- Login and signup pages have identical card-based layouts with shadcn/ui form components
- Dashboard shows icon-labeled navigation cards in a responsive grid
- Settings uses shadcn/ui Select for provider/model selection with proper Card wrapper
- All four pages use semantic color tokens exclusively (no bg-blue-600, no text-neutral-*)
- All existing logic (auth, OAuth, settings save) works identically to before
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-polish-styling/05-02-SUMMARY.md`
</output>
